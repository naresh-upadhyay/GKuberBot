version: "3.9"

services:
  GKuberBot:
    build: .
    container_name: GKuberBot
    env_file:
      - .env
    restart: unless-stopped
    volumes:
      - ./logs:/app/logs
    logging:
      driver: "json-file"
      options:
        max-size: "10m"
        max-file: "3"
